"""Adapters layer — concrete implementations of domain ports.

Adapters import from the domain layer only (they implement domain ports).
They must NEVER be imported by the domain or application layers.
"""
