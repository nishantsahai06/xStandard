"""Infrastructure layer — configuration, wiring, and dependency injection.

This is the ONLY layer that may reference all other layers.  It assembles
the object graph and hands it to the application layer.
"""
